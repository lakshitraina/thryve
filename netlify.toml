# ==============================================================================
# NETLIFY DEPLOYMENT CONFIGURATION — THRYVE FRONTEND
# Proxies to Live Render Backend: https://thryve-9nka.onrender.com
# ==============================================================================

[build]
  publish = "."
  command = "echo 'THRYVE Static Web Build Ready'"

# Proxy API calls directly to live Render backend
[[redirects]]
  from = "/api/*"
  to = "https://thryve-9nka.onrender.com/api/:splat"
  status = 200
  force = true

# Registration page route
[[redirects]]
  from = "/registration"
  to = "/registration.html"
  status = 200

# Analytics Data dashboard route
[[redirects]]
  from = "/data"
  to = "/data.html"
  status = 200

# Fallback for client routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security & Cache Headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=86400, stale-while-revalidate=604800"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=86400, stale-while-revalidate=604800"
